CREATE TABLE recipes(id SERIAL PRIMARY KEY, name VARCHAR NOT NULL UNIQUE);
CREATE TABLE ingredients(id SERIAL PRIMARY KEY, name VARCHAR NOT NULL, recipe_id INT, CONSTRAINT recipe_id_fk FOREIGN KEY (recipe_id) REFERENCES recipes(id) ON DELETE CASCADE);